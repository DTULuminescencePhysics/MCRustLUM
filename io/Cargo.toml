[package]
name = "io"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"

[dependencies]
bincode = "1.3"
common = { path = "../common" }
flate2 = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
